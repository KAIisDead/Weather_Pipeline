# Weather_Pipeline
This is a small Java project I built to practice data engineering basics.  It grabs 7 days of weather data for 5 cities from a free weather API, saves it into a database, cleans it up, and then checks that the data actually makes sense (no missing values, no duplicates, no weird temperatures).
